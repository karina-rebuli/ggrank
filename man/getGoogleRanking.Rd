% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getGoogleRanking.R
\name{getGoogleRanking}
\alias{getGoogleRanking}
\title{Get Google search rank positions of specific url's.}
\usage{
getGoogleRanking(search.words, target.url = NULL, n.pages = 5,
  log.pages = NULL, n.entries = 1, result.type = "organic",
  all.results = FALSE)
}
\arguments{
\item{search.words}{Character value with words to be used on searching. Mandatory.}

\item{target.url}{Character vector with target domains to look for. If NULL, it'll return all entries. Otherwise,
it should be a character vector with length between 1 and 30. Deafult \code{NULL}.}

\item{n.pages}{Integer value with number of search results pages to look at. It should be set between 1 and
30. Default \code{5}.}

\item{log.pages}{Character value with path to save search results pages. If NULL, they're not saved.
Default \code{NULL}.}

\item{n.entries}{Numeric, positive. If target.url is not NULL, how many entries of target-urls must be saved.
Default \code{1}.}

\item{result.type}{Character value with which searching result type should be read, only organic ('organic' or 'o'),
only cpc ('cpc' or 'c') or both ('both' or 'b'). Default \code{organic}.}

\item{all.results}{Logical value to be used with target.url. If FALSE, it'll resturn only info about target urls.
Otherwise, return all results. Default \code{FALSE}.}
}
\value{
A data.frame with founded url, page number, position and, if it should look for target urls, target-url.
}
\description{
Get Google search rank positions of specific url's.
}
\examples{
\dontrun{
getGoogleRanking( search.words = "r project", target.url = c("berkeley.edu", "r-project.org" ) )
getGoogleRanking( search.words = "r project", n.pages = 5, target.url = c("berkeley.edu", "r-project.org" ), result.type = "c" )
getGoogleRanking( search.words = "r project", n.pages = 3, all.results = TRUE )
}

}
